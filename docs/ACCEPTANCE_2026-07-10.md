# 验收记录 — 2026-07-10

环境：macOS、本地 TWS Live `127.0.0.1:7496`、只读 API、延迟行情类型 3、SQLite 验收库。

## 自动化结果

- 后端 Ruff：通过。
- 后端 Pytest：8 项通过，包括真实同步清除演示 K 线、300 日裁剪和周末调度计算。
- Alembic：空 SQLite 数据库升级到 `20260709_01 (head)`。
- 前端：`npm ci`、TypeScript 检查和 Vite 生产构建通过。
- npm production audit：0 个漏洞。

## 真实 IBKR 整链路

- TWS 连接：通过，AAPL 合约 ID `265598`。
- S&P 500 市值前 300 股票池：注册 300、取得真实日线 300、失败 0。
- 全量回填：共保存 89,718 根日线；299 个标的各保留 300 根，2026 年新上市的 HONA 有 18 根可用历史。
- 全量日期范围：`2025-04-29` 至 `2026-07-10`；定时器在 `America/Chicago` 工作日 15:00 增量更新。
- 一年日线：收到并插入 251 根，范围 `2025-07-10` 至 `2026-07-09`。
- 重复同步：再次收到 251 根，插入 0 根，幂等性通过。
- 数据来源切换：真实同步会删除同标的全部 `DEMO` K 线，防止合成日期与交易所日历混合。
- OHLCV：日期有序且唯一，价格关系和非负成交量检查通过。
- 扫描：任务完成并产生候选；按 `scan_job_id` 查询无跨任务泄漏。
- 规则测试：统一信号字段检查通过。
- 回测：23 笔趋势回踩样本；同一标的持仓不重叠。

## 浏览器验收

- Dashboard：显示 IBKR 在线、300 个股票、15:00 自动更新状态、规则和扫描候选。
- Scanner：显示 300 个标的，ticker/公司搜索过滤通过，并可从 UI 发起扫描。
- Chart：NVDA 显示 `300/300 日`真实 IBKR 日线、300 标的切换器及 Entry、Stop、Target、EMA/回踩区标注。
- Rule Library：8 条规则均展示参数、课程资料文件和页码证据。
- Backtest：从 UI 运行默认规则并生成 6 笔逐笔交易结果。
- 浏览器控制台：无应用错误；React Router 未来版本提示已修复并回归。

项目保持只读研究模式；验收未调用任何下单接口。

## 已知验收边界

- 本机没有 Docker CLI，因此未运行 Docker Compose；CI 仅负责构建前后端镜像，不能替代 Compose 运行时验收。
- Live IBKR 数值来自操作者在本地 TWS 会话执行验收脚本的记录；仓库不保存账户信息、原始 broker 日志或验收数据库。
- Compose 中 PostgreSQL 用户名和密码是公开的本地开发默认值，生产部署必须替换。
- 当前测试重点覆盖核心流程、扫描任务隔离、回测入场/持仓不重叠和 IBKR 事件循环；七条规则仍需补充更多逐规则行为测试，前端主要由生产构建和浏览器 E2E 验收覆盖。
